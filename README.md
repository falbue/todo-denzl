# TODO-лист приложение

Современное веб-приложение для управления задачами с Flask, SQLite3 и системой авторизации пользователей.

## Возможности

- 👤 **Система авторизации** - регистрация и вход пользователей
- 🔒 **Личные данные** - каждый пользователь видит только свои задачи
- ✅ Добавление, редактирование и удаление задач
- 🔄 Переключение статуса задач (В работе / Выполнено)
- 📊 Сортировка по дате, статусу и названию
- ✨ 6 красивых тем оформления
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Плавные анимации и переходы
- 🔒 Валидация на клиенте и сервере
- 🎯 Современный UI с Tailwind CSS

## Темы оформления

1. **Морская** - освежающие голубые тона
2. **Orange Juice** - яркие оранжевые цвета
3. **Тиффани** - элегантные бирюзовые оттенки
4. **Тёмная** - для работы в ночное время
5. **Светлая** - классический светлый интерфейс
6. **Пшеница** - тёплые бежевые тона

## Установка и запуск

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Запустите приложение

```bash
python app.py
```

### 3. Откройте в браузере

```
http://127.0.0.1:5000
```

## Структура проекта

```
todo-denzl/
├── app.py                 # Flask приложение с авторизацией
├── database.db           # SQLite база данных (создается автоматически)
├── requirements.txt      # Зависимости Python
├── .env                  # Переменные окружения (пример: DB_PATH=data/database.db)
├── templates/
│   ├── index.html       # Главная страница TODO
│   ├── login.html       # Страница входа
│   └── register.html    # Страница регистрации
└── static/
    ├── css/
    │   └── style.css    # Стили и темы
    └── js/
        ├── main.js      # JavaScript логика TODO
        └── auth.js      # JavaScript авторизации
```

## Технологии

- **Backend**: Flask 3.0, SQLite3
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Стили**: Tailwind CSS + кастомные CSS переменные
- **Иконки**: Font Awesome 6.4

## API Endpoints

### Авторизация
- `POST /api/register` - Регистрация пользователя
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход из системы

### Задачи (требуется авторизация)
- `GET /api/tasks` - Получить задачи пользователя
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/<id>` - Обновить задачу
- `PATCH /api/tasks/<id>/status` - Переключить статус
- `DELETE /api/tasks/<id>` - Удалить задачу

## Особенности

- **Многопользовательская система** - каждый пользователь имеет свои задачи
- **Безопасность** - пароли хешируются с помощью Werkzeug
- **Сессии** - автоматическое сохранение сессии на 24 часа
- **Без jQuery** - чистый современный JavaScript
- **Адаптивный дизайн** - работает на всех устройствах
- **Сохранение темы** - выбранная тема сохраняется в localStorage
- **Валидация** - защита от некорректных данных
- **Плавные анимации** - приятный UX
- **Чистый код** - с комментариями

## Разработчик

Создано с ❤️ для эффективного управления задачами.

## Лицензия

MIT License - свободно используйте в своих проектах!
